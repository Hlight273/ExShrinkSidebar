<Window x:Class="ExShrinkSidebar.UI.Views.AddConfigView.AddConfigPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExShrinkSidebar.UI.Views.MainWindow"
        mc:Ignorable="d"
        Title="添加配置" Height="280" Width="400"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- 简单的按钮样式：去除默认背景，鼠标悬停变亮 -->
        <Style x:Key="CleanButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- 窗口容器：圆角背景 -->
    <Border CornerRadius="10" Background="#FF2D2D30" BorderBrush="#FF688BC3" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="OnDragMove">
                <TextBlock Text="添加配置" Foreground="White" Margin="15,0,0,0" VerticalAlignment="Center" FontSize="14"/>

                <!-- 关闭按钮 (使用样式) -->
                <Button x:Name="CloseBtn" Content="✕" 
                        Width="30" Height="30" 
                        Click="OnCloseClick"
                        Style="{StaticResource CleanButtonStyle}"
                        Foreground="#FFAAAAAA" FontSize="14" 
                        HorizontalContentAlignment="Center" 
                        VerticalContentAlignment="Center"
                        HorizontalAlignment="Right" Margin="0,0,10,0"/>
            </Grid>

            <!-- 内容区域 -->
            <StackPanel Grid.Row="1" Margin="20,0,20,10">
                <!-- 配置类型 -->
                <TextBlock Text="配置类型" Foreground="White" Margin="0,5"/>
                <ComboBox x:Name="TypeComboBox" Height="30" VerticalContentAlignment="Center" SelectedIndex="0"/>

                <!-- 名称 -->
                <TextBlock Text="名称" Foreground="White" Margin="0,15,0,5"/>
                <TextBox x:Name="NameTextBox" Height="30" VerticalContentAlignment="Center" 
                         Padding="5,0" Background="#FF3E3E42" Foreground="White" 
                         BorderBrush="#FF688BC3" BorderThickness="1"
                         Text="">
                    <TextBox.Resources>
                        <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock Text="未命名" Foreground="Gray"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=NameTextBox}" Value="">
                                    <Setter Property="Background" Value="{StaticResource HintText}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>

            <!-- 底部按钮 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,15">
                <Button x:Name="ConfirmBtn" Content="确认" 
                        Width="60" Height="28" 
                        Click="OnConfirmClick"
                        Background="#FF688BC3" Foreground="White" 
                        Margin="0,0,10,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>